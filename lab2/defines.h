
#include <stdint.h>

volatile void StrCopyRev(uint8_t * Array_1, uint8_t * Array_2);

volatile uint64_t SumOfUnsigned(uint32_t * Array, uint8_t size);

volatile int64_t SumOfSigned(int32_t * Array, uint32_t size);
